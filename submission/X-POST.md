Built BreakGlass on @terminal3io: an AI agent can revoke one exact deploy key during a specific incident without ever receiving the GitHub admin credential.

The authority is short-lived and one-use. The live run was independently verified before and after, then a replay returned REPLAY_REFUSED with zero destructive calls.

The agent supplied only an incident ID.
